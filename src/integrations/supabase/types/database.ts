export type Json =
  | string
  | number
  | boolean
  | null
  | { [key: string]: Json | undefined }
  | Json[]

export interface Database {
  public: {
    Tables: {
      follows: TablesDefinition['follows'];
      likes: TablesDefinition['likes'];
      posts: TablesDefinition['posts'];
      profiles: TablesDefinition['profiles'];
      comments: TablesDefinition['comments'];
    };
    Views: {
      [_ in never]: never;
    };
    Functions: {
      [_ in never]: never;
    };
    Enums: {
      [_ in never]: never;
    };
    CompositeTypes: {
      [_ in never]: never;
    };
  };
}